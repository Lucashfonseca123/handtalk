import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#DDDDDD',
  },
  subTitle: {
    marginLeft: 8, marginTop: 8, fontSize: 15, fontWeight: '500',
  },
});

